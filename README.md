# Regex Rule Workflow (Front-end)

打开 `index.html` 即可使用，无需后端。

功能：
- 拆解学生代码的可编辑区（frontCode / rearCode）
- 按行生成规则建议
- 规则编辑与实时验证（Y 必须命中 / N 必须不命中）
- noticeDict JS 片段输出
- Regex Lab：类似 regex101 的单独验证区

数据保存在浏览器本地存储中。
